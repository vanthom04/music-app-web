const routes = {
  home: '/',
  search: '/search',
  upload: '/upload',
  liked: '/liked',
  playlist: '/playlist/:playlistId',
  profile: '/profile',
  settings: '/settings',
  notFound: '*'
}

export default routes
